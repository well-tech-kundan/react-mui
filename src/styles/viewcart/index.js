
import { styled } from "@mui/material/styles";
import { Box } from "@mui/system";


//define the store container, note BOX is like div in html
export const ViewCartContainer = styled(Box)(() => ({

    display: 'grid',
}));


  